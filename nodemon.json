{
  "watch": ["src", ".env"],
  "ext": "ts,json,env",
  "ignore": ["src/**/*.spec.ts", "src/seed.ts"],
  "exec": "ts-node src/server.ts",
  "signal": "SIGTERM",
  "delay": 500,
  "env": {
    "NODE_ENV": "development"
  }
}
